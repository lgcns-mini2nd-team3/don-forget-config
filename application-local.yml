eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  datasource:
    url: jdbc:mariadb://localhost:3306/team3?useSSL=false
    username: root
    password: myky0226@
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379
      password:
      timeout: 6000s

  cloud:
    bus:
      enabled: true