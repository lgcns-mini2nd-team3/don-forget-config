eureka:
    client:
        enabled: false

spring:
    rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest

    datasource:
        url: jdbc:mysql://localhost:3307/inspire?useSSL=false
        username: root
        password: 123456789

    jpa:
        database: mysql
        database-platform: org.hibernate.dialect.MariaDBDialect
        hibernate:
            ddl-auto: create
        show-sql: true
            
    data:
        redis:
            host: redis-service
            port: 6379
            password: 
            timeout: 6000s
            
    cloud:
        bus:
            enabled: true